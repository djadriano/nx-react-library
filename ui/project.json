{
  "name": "ui",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "ui/src",
  "projectType": "library",
  "tags": [],
  "targets": {
    "build": {
      "inputs": ["production", "^production"],
      "outputs": ["{workspaceRoot}/ui/dist"],
      "dependsOn": ["^build"],
      "options": {}
    },
    "nx-release-publish": {
      "options": {
        "packageRoot": "{project}/dist"
      }
    }
  }
}
